# RollBounce Leaderboard API Environment Variables

# Database connection (Render will provide this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# Node environment
NODE_ENV=production

# Port (Render sets this automatically)
PORT=3000
